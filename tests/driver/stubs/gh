#!/bin/sh
# gh stub — pr create: succeed with a fake URL, or fail on demand
if [ "${STUB_GH_FAIL:-0}" = 1 ]; then
  echo 'gh stub: refusing to open the PR' >&2
  exit 1
fi
echo 'https://example.invalid/pr/1'
